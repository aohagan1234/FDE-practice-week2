# D3 — Agentic Solution Architecture + ADRs
**Paste in:** D1 + D2 outputs.

---

## Prompt

```
Using the problem framing and scope below, produce:

[PASTE D1 + D2 HERE]

**Workflow delegation map** — table:
| Workflow step | Current method | Delegation level | Reason |

Delegation levels: Fully Agentic | Agent-led + human oversight | Human-led + automation support | Human only.
The solution must be AI-native. At least one step must be Fully Agentic with a clear reason an agent — not a rule — is the right mechanism.

**ADR 1 — [name the decision]**
- Decision:
- Alternative A considered: [name it] — consequences if chosen:
- Alternative B considered: [name it] — consequences if chosen:
- Why we chose what we chose:
- Conditions that would make us revisit this:

**ADR 2 — [name the decision]**
- Decision:
- Alternative A considered: [name it] — consequences if chosen:
- Alternative B considered: [name it] — consequences if chosen:
- Why we chose what we chose:
- Conditions that would make us revisit this:
```

---

**Output should be:** 1 workflow table + 2 ADRs in the exact structure above. No narrative. No justification-only ADRs.
